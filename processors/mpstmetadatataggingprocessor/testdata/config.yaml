receivers:
  nop:

processors:
  mpstmetadatatagging:
    roles:
      client:
        name: frontend

exporters:
  nop:

service:
  pipelines:
    traces:
      receivers:
      - nop
      processors:
      - mpstmetadatatagging
      exporters:
      - nop
