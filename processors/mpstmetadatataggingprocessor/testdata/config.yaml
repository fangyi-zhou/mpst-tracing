receivers:
  nop:

processors:
  mpstmetadatatagging:

exporters:
  nop:

service:
  pipelines:
    traces:
      receivers:
      - nop
      processors:
      - mpstmetadatatagging
      exporters:
      - nop
