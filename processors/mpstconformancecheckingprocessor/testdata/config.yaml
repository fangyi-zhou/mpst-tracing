receivers:
  examplereceiver:

processors:
  mpstconformancechecking:
    protocol: gtype.sexp

exporters:
  exampleexporter:

service:
  pipelines:
    traces:
      receivers:
      - examplereceiver
      processors:
      - mpstconformancechecking
      exporters:
      - exampleexporter
