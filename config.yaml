receivers:
  otlp:
    protocols:
      grpc:
        endpoint: localhost:55680

processors:

exporters:
  logging:
  mpstconformancemonitoring:
    protocol: twobuyer/TwoBuyer_global_type.sexp

service:
  pipelines:
    traces:
      receivers:
      - otlp
      processors:
      exporters:
      - logging
      - mpstconformancemonitoring
