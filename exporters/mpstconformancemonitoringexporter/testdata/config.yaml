receivers:
  examplereceiver:

processors:
  exampleprocessor:

exporters:
  mpstconformancemonitoring:
    semantic_model_type: gtype-lts
    protocol_sexp_filename: gtype.sexp

service:
  pipelines:
    traces:
      receivers:
      - examplereceiver
      processors:
      - exampleprocessor
      exporters:
      - mpstconformancemonitoring
