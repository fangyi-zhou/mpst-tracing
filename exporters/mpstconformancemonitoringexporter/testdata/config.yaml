receivers:
  examplereceiver:

processors:
  exampleprocessor:

exporters:
  mpstconformancechecking:
    protocol: gtype.sexp

service:
  pipelines:
    traces:
      receivers:
      - examplereceiver
      processors:
      - exampleprocessor
      exporters:
      - mpstconformancemonitoring
