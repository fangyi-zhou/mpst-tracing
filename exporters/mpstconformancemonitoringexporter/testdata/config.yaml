receivers:
  examplereceiver:

processors:
  exampleprocessor:

exporters:
  mpstconformancemonitoring:
    protocol: gtype.sexp

service:
  pipelines:
    traces:
      receivers:
      - examplereceiver
      processors:
      - exampleprocessor
      exporters:
      - mpstconformancemonitoring
